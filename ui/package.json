{
  "name": "@mahjongsolitaire/ui",
  "version": "0.1.0",
  "description": "Rendering, input, and (from issue #12) accessibility for the browser build (spec §9 /ui).",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ../core run build && vite",
    "build": "npm --prefix ../core run build && tsc && vite build",
    "preview": "vite preview",
    "test": "npm --prefix ../core run build && tsc && node --test \"dist/test/*.test.js\""
  },
  "dependencies": {
    "@mahjongsolitaire/core": "file:../core",
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "playwright-core": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^6.3.6"
  }
}
