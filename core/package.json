{
  "name": "@mahjongsolitaire/core",
  "version": "0.1.0",
  "description": "Pure game logic for Mahjong Solitaire: deterministic, zero platform deps (spec §9).",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./dist/src/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test \"dist/test/*.test.js\"",
    "soak": "tsc && node dist/soak.js"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0"
  }
}
